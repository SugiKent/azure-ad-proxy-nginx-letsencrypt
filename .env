# Azure AD Settings
CLIENT_ID=<AzureAD_Application_ID>
CLIENT_SECRET=<AzureAD_Client_Secret>
AZURE_TENANT=<AzureAD_Tenant_ID>

# OAuth2 Proxy Settings
COOKIE_SECRET=<Generate_with_openssl_rand_-base64_32>
DOMAIN=<your_domain.com>

# Backend Application Settings
BACKEND_HOST=<backend_service_name_or_ip>
BACKEND_PORT=<backend_port>
BACKEND_NETWORK=<existing_network_name>
USE_EXTERNAL_NETWORK=true

# Nginx Settings
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Authentication Skip Paths
AUTH_SKIP_PATHS=/public|/api/health|/static

# Instructions on how to obtain Azure AD settings values
# CLIENT_ID: This is the Application (client) ID found on the application's Overview page in the Azure portal.
# CLIENT_SECRET: This is the value of the client secret you created in the Certificates & secrets page.
# AZURE_TENANT: This is the Directory (tenant) ID found on the application's Overview page in the Azure portal.
